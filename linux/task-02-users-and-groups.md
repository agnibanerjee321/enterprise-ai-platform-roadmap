# Task 2.2 — Linux Users and Groups

## Goal

Understand Linux users, groups, user IDs, group IDs, and where account information is stored.

## Commands Practiced

```bash
whoami
id
groups
groups agni
cat /etc/passwd | head
cat /etc/group | head
grep agni /etc/passwd
grep agni /etc/group
ls -l /etc/shadow